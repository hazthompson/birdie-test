{
  "name": "birdie-test",
  "version": "0.0.0",
  "scripts": {
    "start": "cd backend && npm start",
    "heroku-postbuild": "cd backend && npm install --only=prod && cd ../frontend && npm install --only=prod && npm run build"
  },
  "engines": {
    "node": ">=10.0.0"
  }
}
